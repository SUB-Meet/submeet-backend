package submeet.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import submeet.backend.entity.PostEndStation;

public interface PostEndStationRepository extends JpaRepository<PostEndStation,Long> {
}
